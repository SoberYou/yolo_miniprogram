<view class="bottom-nav">
  <view wx:for="{{list}}" wx:key="index" class="nav-item {{selected === index ? 'active' : ''}}" data-path="{{item.pagePath}}" data-index="{{index}}" bindtap="switchTab">
    <text>{{item.text}}</text>
  </view>
</view>